rdf imwrite {
    topology {
        nodes = A,B;
	edges = e1(A,B);
    }
    actor A {
    	computation = VideoCapture;
	file_name = pedestrian.mp4;
    }
    actor B {
    	computation = ImageWrite;
    }
}

