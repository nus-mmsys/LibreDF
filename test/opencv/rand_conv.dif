df rand_conv {
    topology {
        nodes = A,B,C;
	edges = e1(A,B), e2(B,C);
    }
    actor A {
    	computation = Randu;
    }
    actor B {
    	computation = Convolution;
    }
    actor C {
    	computation = ImageWrite;
    }
}
