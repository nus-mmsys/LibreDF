df imread_imwrite {
    topology {
        nodes = A,B;
	edges = e1(A,B);
    }
    actor A {
    	computation = ImageRead;
	dataset = ImageNet;
    }
    actor B {
    	computation = ImageWrite;
    }
}
